# Local SoundFonts

Place user-supplied `.sf2` files here when developing a personal factory pack.
SoundFonts are ignored by Git and are not distributed with sndj. The browser
patcher is the preferred way to import them and export
`factory/factory.sndjfact`.
