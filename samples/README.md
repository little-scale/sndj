# Local samples

This directory is for user-supplied sample sources and is ignored by Git.
Only add audio that you have permission to use. No sample recordings ship with
sndj; a clean build uses the synthesized placeholder pool generated by
`tools/sndj_pool.py`.
